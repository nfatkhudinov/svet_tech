import React from 'react';
import likeButton from "./_button_like.module.scss"
const ButtonLike = () => {
    return (
        <div className={likeButton.container}/>
    );
};

export default ButtonLike;